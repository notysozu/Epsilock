<h1>Incidents</h1>
<section class="card">
  <form method="get" action="/admin/incidents" class="row">
    <input name="type" placeholder="type" value="<%= filter.type || '' %>">
    <select name="severity">
      <option value="">all severity</option>
      <option value="low">low</option><option value="medium">medium</option><option value="high">high</option><option value="critical">critical</option>
    </select>
    <button type="submit">Filter</button>
  </form>
</section>
<section class="card cascading-item">
  <div class="table-wrap">
    <table class="data-table">
      <thead><tr><th>ID</th><th>Type</th><th>Severity</th><th>User</th><th>Node</th><th>IP</th><th>Action</th><th>Time</th></tr></thead>
    <tbody>
      <% incidents.forEach((i) => { %>
        <tr>
          <td style="white-space: nowrap;"><%= i.incidentId %></td>
          <td><%= i.type %></td>
          <td><%= i.severity %></td>
          <td><%= i.userId || '-' %></td>
          <td><%= i.nodeId || '-' %></td>
          <td><%= i.sourceIp || '-' %></td>
          <td><%= i.actionTaken %></td>
          <td><%= new Date(i.createdAt).toLocaleString() %></td>
        </tr>
      <% }) %>
    </tbody>
  </table>
  </div>
</section>
